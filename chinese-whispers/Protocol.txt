telnet: open [\\RemoteServer] [Port]

Se deberia aclarar la version del protocolos
add a command to add users.

MPLX userJid remoteServer [port] ??
Port   : Specifies the port that you want to use. If you do not specify a port, the default port is assumed.

add: LANG xxx para setear el lenguage.
LANG te tira lo soportado
LANG xxx te lo cambia a eso.

Aclarar en el protocolo cual de los errores es prioritario (ejemplo wrong parameters vs unauthorized vs unexpected method)
//TODO tiene que ver detalles de impelemtancion con el protocolo? Ejemplo l337 en medio de un msj.
respuestas:

//Correct:
"OK" 100

//User errors:
UNAUTHORIZED. "Needs to LogIn using AUTH command"           200
FORBIDDEN     "Needs more priviledge to do this operation"  201
WRONG USERNAME/PASSWORD: FAILURE                            202
Must QUIT to logIn again: UNEXPECTED COMMAND//Method not allowed 203
WRONG NUMBER OF PARAMETERS                                  204
WRONG SYNTAX OF PARAMETERS                                  205
UNKNOWN COMMAND                                             206 //Desconoce el comando
Too Many Requests "Hay muchas conexiones desde esta dirección de internet." 207
"Unimplemented metric": NOT FOUND  208
//Entiende el comando y esta implementado pero el comando pide que haga algo que no existe (devolver una metrica inexistente)

//Server errors
COMMAND NOT IMPLEMENTED                                     304 //Sabe que existe ese comando pero no lo implementó. Tiene sentido? Es error del server?
Internal Server Error                                       301
Service Unavailable                                         302
Protocol Version Not Supported                              303

""

TODO:
add Lang in protocol to ISO 639-1 Language Codes
Close inactive connexions
Do not accept multiple conexion from same host?
Measure max connexion, etc...
check where DEFAULT host and port
java.nio.channels.UnresolvedAddressException when error in address.

check to send correct error and to send them and close correctly.
re use all stringBuilders
check capitalization in protocol

TODO neg:

check bullshit
send error if message is bloqued.



TODO PROTOCOL:
maximun number of client to be silenced //Size of clients names, etc...
chenge write to be nonBlcking and to work like the handlers.
finish mtrcs.
set maximum size for request.
talk in protocol about spaces to right that are not important and amout of spaces and bla bla?
block one user to other interation?
 limitar el tamaño de los nombres de clientes y de servers y la cantidad.                           X

marcar como finished cuando leamos un </stream>                              X
check if we can send an message without "from" to client in neg.             X
tell error to client if server error.                                        X

negociacion compleja.
errores?
Loggin?

log4j2 async: buen ciudadano
no ahcer la copia de byte[]
buffer 8k

desvantaja informe: regenerar todos los xml

re usar string builder.
no colas infinitas
maquinas de estado.
IBB
nc dibujar vel

No soportar challenge.

tirar error si no responde durante neg?











